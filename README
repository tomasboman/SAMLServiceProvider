This project is a simple SAML Service Provider that runs as a Spring application on top of Cloud Foundry.

To run in STS, open project and convert to Maven project.  Then add Cloud Foundry runtime in the build path.